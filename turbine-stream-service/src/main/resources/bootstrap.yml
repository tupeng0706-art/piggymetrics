spring:
  application:
    name: turbine-stream-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URI:http://config:8888}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user